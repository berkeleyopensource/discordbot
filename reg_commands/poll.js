export default {
    name: poll,
    description: 'starts a poll',
    args: true,
    flexArgs: true,
    usage: '([poll item 1]) ([poll item 2]) ...',
    cooldown: 10,
    execute(message, args, bot) {
        
    }
}
